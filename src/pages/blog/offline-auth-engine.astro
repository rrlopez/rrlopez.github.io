---
import BlogLayout from '../../layouts/BlogLayout.astro';
import offline_auth_engineHtml from '../../content/blog/offline-auth-engine.html?raw';
---

<BlogLayout title={"Auth That Works Offline — Building a Credential Cache with Better Auth and SQLite OPFS | RJ Lopez"} description={"How NUVELCO CCA implements dual-mode authentication: Better Auth handles the online session, a locally-persisted SQLite credential cache with crypto.subtle hashing handles the offline path — with a shared AuthEngine object coordinating both."}>
  <Fragment set:html={offline_auth_engineHtml} />
</BlogLayout>
